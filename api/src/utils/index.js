const cache = require('./cache')
const hasBody = require('./has-body')
const hasParam = require('./has-param')
const hasQuery = require('./has-query')

module.exports = {
  cache,
  hasBody,
  hasParam,
  hasQuery
}
